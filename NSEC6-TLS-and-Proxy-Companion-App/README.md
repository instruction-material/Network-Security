# NSEC6: TLS and Proxy Companion App

Audit reverse-proxy and TLS configuration choices before exposing a service.

## Goals

- reason about trusted proxy headers
- distinguish internal and public listeners
- flag weak transport or exposure assumptions
